"use client";

import { apiQueryClient } from "./react-query-client";

export function TestClient() {
  // const { data, isLoading } = apiQueryClient.user.getAll.useQuery(["users"]);
  // if (isLoading) return <p>Loading...</p>;
  // return (
  //   <ul>
  //     {data?.body.map((u) => <div className="text-red-500">{u.username}</div>)}
  //   </ul>
  // );
  return <></>;
}
